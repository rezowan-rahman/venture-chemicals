<form action="{{ path('SettingsConfigBundle_test_procedure_add', {'id': id}) }}" method="post" {{ form_enctype(form) }} class="test_procedure">
    {{ form_widget(form) }}
    <p>
        <input class="config_form_button" type="submit" value="Save">
    </p>
</form>