<form action="{{ path('SettingsConfigBundle_shipping_method_add', {'id': id}) }}" method="post" {{ form_enctype(form) }} class="shipping_method">
    {{ form_widget(form) }}
    <p>
        <input class="config_form_button" type="submit" value="Save">
    </p>
</form>